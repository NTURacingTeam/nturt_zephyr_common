menuconfig P51_CURR
    bool "SSI Technologies P51 series pressure sensor with current output"
    depends on DT_HAS_SSI_P51_CURR_ENABLED
    default y
    select ADC
    help
      Enable driver for P51 series pressure sensor.

if P51_CURR

endif # P51_CURR
