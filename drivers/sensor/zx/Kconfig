menuconfig ZX
    bool "RES Instrument ZX series linear traveral sensor"
    depends on DT_HAS_RES_ZX_ENABLED
    default y
    select ADC
    help
      Enable driver for ZX series linear traveral sensor.

if ZX

endif # ZX
